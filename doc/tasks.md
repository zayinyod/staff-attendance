## Priority.
- 各月の一覧画面


## Task.
- パスワード バリデーション
- パスワード再設定
- アクセス履歴
- ダッシュボード（打刻データの一覧）
- Userクラスにアイコン追加
- inしてないのにoutするとエラーを返す（24時を超えたらどうなるか）
- ログをDB or fileに保存
- inしてるのにoutしてない場合に通知する
- user idとEmailでもログイン可能にする
- ログインロック機能
- modelformをforms.formに修正（get_form_kwargs()でドメインから値を取得）
- サイドバーのメニューを選択してもサイドバーが消えないように修正
- サイドバーの選択したメニューをactiveにする
- サイト右上にユーザアイコン表示
- サイドバーのメニューにアイコン付与
- サイドバーの範囲修正（サイドバーの上部下部がメインメニュー扱いになっている）
- usernameのUnique制約を修正
- ビジネスロジック計算結果 テスト用バッチ作成
- nowに曜日出力を追加
- ユーザ登録時に登録成功画面に遷移
- 登録成功画面でuser idを通知する + 警告
- 各workflowテーブルを統合するテーブル
- workflowのページは、申請 or 承認を選択する画面（superuserのみアクセス可）から各画面に遷移させる
- superuserのみアクセス可能な承認画面の作成
- administrationでメールアドレスを変更可能にする
- 申請（スーパーユーザ権限）
- nowにyear、monthを追加
- render or AWSでデプロイ
- form入力後に確認画面でOKを押した後で初めてsave処理が走るように修正
- 当日の打刻OUT時にIN時の情報を照会する設計を、当日以外の日でも行えるように修正
- 承認のinputの値をPending以外にすると色が変わる
- now.pyの修正（文字列型とdatetime型で関数を分ける）
- 承認画面のCSS整理
- 通常ユーザとスーパーユーザでViewの基底クラスを作成
